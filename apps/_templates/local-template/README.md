# Local Template

1. Copy this folder.
2. Rename it to `local-<slug>`.
3. Replace `workflow.app.json` with the real exported ComfyUI app file.
4. Update `item.json`.
5. Add `cover.jpg`.
6. Add the item to `manifest.json`.
